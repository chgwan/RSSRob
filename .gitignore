# AI / agent artifacts (kept out of the repo, per project preference)
CLAUDE.md
AGENT.md
docs/superpowers/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Test / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/
ENV/

# RSSRob generated output / runtime state
feeds/
*.db
# generated by tools/select_preview.py (root only — not web/templates/preview.html)
/preview.html
# user-submitted emails — never commit
subscribers.json
# per-feed "already emailed" item ids for incremental digests (runtime state)
digest_state.json

# Local secrets / dotenv files (never commit). .env.example is the committed
# template (not matched by these patterns).
.env
*.env

# OS / editor cruft
.DS_Store
*.swp
.vscode/
.idea/
